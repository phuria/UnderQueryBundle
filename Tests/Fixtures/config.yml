phuria_under_query:
  connections:
    - {service: database_connection}

doctrine:
  dbal:
    connections:
      default:
        dbname: %database.dbname%
        user: %database.user%
        password: %database.password%
