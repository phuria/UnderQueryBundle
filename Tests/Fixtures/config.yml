phuria_under_query:
  connection: database_connection

doctrine:
  dbal:
    connections:
      default:
        host: %database.host%
        dbname: %database.dbname%
        user: %database.user%
        password: %database.password%
